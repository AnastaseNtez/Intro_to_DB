-- SQL script to list all tables in the database specified at runtime.
-- When running the mysql command, the database name (e.g., alx_book_store)
-- should be passed as an argument, making the USE command optional.

SHOW TABLES;